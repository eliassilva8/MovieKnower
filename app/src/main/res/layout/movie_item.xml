<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:tools="http://schemas.android.com/tools" xmlns:android="http://schemas.android.com/apk/res/android" xmlns:app="http://schemas.android.com/apk/res-auto">
    <data>
        <variable name="movie" type="com.ems.movieknower.data.Movie"/>
    </data>

    <android.support.v7.widget.CardView android:layout_width="match_parent"
                                        android:layout_height="wrap_content"
    android:layout_margin="4dp">

    <android.support.constraint.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

        <ImageView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:src="@drawable/ic_launcher_background"
                android:id="@+id/movies_poster"
                android:elevation="2dp" android:contentDescription="@string/movie_poster"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintStart_toStartOf="parent"/>
        <TextView
                android:text="@{movie.rating}"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:id="@+id/movie_rating"
                app:layout_constraintEnd_toEndOf="@+id/movies_poster"
                android:layout_marginEnd="8dp"
                app:layout_constraintBottom_toTopOf="@+id/movie_title"/>
        <TextView
                android:text="@{movie.title}"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:id="@+id/movie_title"
                android:gravity="center_horizontal|center_vertical"
                android:maxLines="1"
                app:layout_constraintTop_toBottomOf="@+id/movies_poster"
                app:layout_constraintEnd_toEndOf="@+id/movies_poster"
                app:layout_constraintStart_toStartOf="@+id/movies_poster" android:layout_marginTop="4dp"
                app:layout_constraintBottom_toBottomOf="parent" android:layout_marginBottom="8dp"/>

    </android.support.constraint.ConstraintLayout>
    </android.support.v7.widget.CardView>
</layout>